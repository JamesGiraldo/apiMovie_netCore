namespace ApiMovies.Models.Enums;

// Clasificación por edad sugerida (etiquetas numéricas alineadas a restricciones de contenido).
public enum TypeClassification
{
    // Contenido para todo público / clasificación 7+ (según convención del dominio).
    Siete = 1,
    Trece = 2,
    Dieciseis = 3,
    Diesiocho = 4
}
